// Data.ts

// Définition de l'interface Data
export interface Data {
  id: string;
  num_slide: number;
  id_cours: string;
  list: any[]; // Vous voudrez peut-être remplacer 'any[]' par un type plus spécifique
}
